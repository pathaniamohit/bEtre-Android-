<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Statistics Section -->
    <TextView
        android:id="@+id/flagged_content_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Flagged Content Statistics"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:id="@+id/total_flagged_posts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total Flagged Posts: 0"
        android:textSize="16sp"
        android:layout_marginBottom="8dp"/>

    <!-- RecyclerView to Display Flagged Content -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewFlaggedPosts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="16dp"/>
</LinearLayout>